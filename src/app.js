import { T } from './js/main'
import './styles/styles.scss'

T.Slider.init('#slider', {})
T.Collapsible.init('#collapsible', {})
T.Dropdown.init('#dropdown', {})
T.TapTarget.init('#tap', {})
T.ImageBox.init('#boxed', {})
T.Modal.init('#modal', {})
// T.ScrollSpy.init('#scrollSpy', {})
T.Sidenav.init('#sidenav', {})
T.Tabs.init('#tabs', {})
T.Toast.init('#toast', {})
T.Waves.init('.wave', {})
T.Paralax.init('.paralax', {})

// const touch = document.querySelector('.touch')
// touch.addEventListener('touchstart', touchstart)
// touch.addEventListener('touchend', touchend)
// touch.addEventListener('touchmove', touchmove)

// function touchstart(e) {
//   touch.insertAdjacentText('beforeend', 'start ')
// }

// function touchend(e) {
//   touch.insertAdjacentText('beforeend', 'end ')
// }
// function touchmove(e) {
//   touch.insertAdjacentText('beforeend', 'move ')
//   console.log(e)
// }
